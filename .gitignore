# Secrets / local config — NEVER commit
.env
.env.*
!.env.example
*.local
config/hosts.json
config/hosts.yaml
# Local Docker Compose override (real hostnames / Traefik labels). Use
# compose.override.example.yaml as the template.
compose.override.yaml

# Node
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
coverage/

# Logs / OS
*.log
npm-debug.log*
.DS_Store

# Editor
.idea/
.vscode/

# Claude local settings (keep .claude/plans tracked)
.claude/settings.local.json

# Developer worktrees (per AGENTS.md make wt convention)
.wt/

# Agent workspace / superpowers plans scratch
docs/superpowers/

