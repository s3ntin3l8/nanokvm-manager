# Local development override — builds the image from source instead of pulling
# from GHCR. Auto-merged by `docker compose up`. Production deploys with the base
# file only: `docker compose -f compose.yaml up -d`.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: nanokvm-manager:local
    pull_policy: build
